{
  "vector": "messaging/commit-retry-blocked-surfaces",
  "format": 1,
  "kind": "messaging",
  "description": "policy.blocked (the commit broke M§7.3 or failed validation) is discarded and surfaced, never retried.",
  "spec_ref": [
    "M§6.5",
    "§15.3"
  ],
  "context": {
    "component": "commit-retry",
    "max_attempts": 3
  },
  "input": {
    "check": "commit-retry-trace",
    "steps": [
      {
        "event": {
          "answer": {
            "reason": "policy.blocked"
          }
        }
      }
    ]
  },
  "expect": {
    "steps": [
      {
        "emit": [
          {
            "discard": {}
          },
          {
            "surface": "policy.blocked"
          }
        ],
        "state": {
          "attempt": 1,
          "state": "surfaced"
        }
      }
    ]
  }
}
