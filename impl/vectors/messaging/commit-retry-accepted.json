{
  "vector": "messaging/commit-retry-accepted",
  "format": 1,
  "kind": "messaging",
  "description": "A commit the hub accepts is merged; nothing else happens.",
  "spec_ref": [
    "M§6.5",
    "§15.3"
  ],
  "context": {
    "component": "commit-retry",
    "max_attempts": 3
  },
  "input": {
    "check": "commit-retry-trace",
    "steps": [
      {
        "event": {
          "answer": {}
        }
      }
    ]
  },
  "expect": {
    "steps": [
      {
        "emit": [
          {
            "merge": {}
          }
        ],
        "state": {
          "attempt": 1,
          "state": "merged"
        }
      }
    ]
  }
}
